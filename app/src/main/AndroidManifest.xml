<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Practika"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"
            tools:ignore="Instantiatable" />

        <activity android:name=".AuthActivity"
            tools:ignore="Instantiatable" />

        <activity android:name=".CreateProfileActivity"
            tools:ignore="Instantiatable" />

        <activity android:name=".CreateProfilePassActivity"
            tools:ignore="Instantiatable" />

        <activity android:name=".CreatePinActivity"
            tools:ignore="Instantiatable" />

        <activity android:name=".EnterPinActivity"
            tools:ignore="Instantiatable" />

        <activity android:name=".HomeFragment"
            tools:ignore="Instantiatable" />

        <activity android:name=".CatalogFragment"
            tools:ignore="Instantiatable" />

        <activity android:name=".ProjectsFragment"
            tools:ignore="Instantiatable" />

        <activity android:name=".ProfileFragment"
            tools:ignore="Instantiatable" />

        <activity android:name=".CreateProjectFragment"
            tools:ignore="Instantiatable" />

        <activity android:name=".ShoppingCartActivity"
            tools:ignore="Instantiatable" />


    </application>

</manifest>